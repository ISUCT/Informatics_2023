package MathInGo

import (
	"fmt"
	"math"
)

func MathInGo() {
	a := 1.35
	b := 0.98
	x := 0.35

	result := math.Cbrt(a*x + b)
	logor := math.Log10(x)
	logorv2 := logor * logor
	itog := result / logorv2
	fmt.Println("Результат второй формулы из таблицы - ", itog)
}
